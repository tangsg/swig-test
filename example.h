#include <algorithm>
#include <iostream>

class Example {
    public:
    void say_hello();
    void add(int x, int y, int *result);
    int sub(int *x, int *y);
    void array_add(int *a, int len_a, int *b, int len_b, int *c, int len_c);
};